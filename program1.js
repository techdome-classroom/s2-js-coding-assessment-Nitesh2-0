/**
 * @param {string} s
 * @return {boolean}
 */
var isValid = function(s) {
    let array = s.split("");
    const mapVal = new map(); 
    map.set('{','}');
    map.set('(',')'); 
    map.set('[',']'); 
    
    let 
};

module.exports = { isValid };


